FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /host ./cmd/host

FROM alpine:latest
COPY --from=builder /host /usr/local/bin/host
EXPOSE 7860
CMD ["host", "-f", "/etc/host.yaml"]
